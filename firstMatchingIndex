class Solution {
    public int firstMatchingIndex(String s) {
        
        int n=s.length();
        for(int i=0;i<s.length();i++){
            int a=n-i-1;
            if(s.charAt(i)==s.charAt(a)){
                return i;
            }
        }
        return -1;
    }
}
